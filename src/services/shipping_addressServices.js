const Shipping_address = require("../models/shipping_address");const aqp = require("api-query-params");const path = require("path"); //fs : file systemconst fs = require("fs");const bcrypt = require("bcrypt");const ppostShipping_addressServices = async (data) => {    try {        let result = await Shipping_address.create(data);        return result;    } catch (error) {        console.log(error);        return null;    }};module.exports = { postShipping_addressServices };